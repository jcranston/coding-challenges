from typing import List


def three_sum(nums: List[int]) -> List[List[int]]:
    """
    User-submitted solution for LeetCode 15: 3Sum.
    Given an integer array nums, return all unique triplets [nums[i], nums[j],
    nums[k]]
    such that i != j, i != k, j != k, and nums[i] + nums[j] + nums[k] == 0.
    The solution set must not contain duplicate triplets.
    Args:
        nums (List[int]): List of integers.
    Returns:
        List[List[int]]: List of unique triplets that sum to zero.
    """
    pass


def canonical_three_sum(nums: List[int]) -> List[List[int]]:
    """
    Canonical solution for LeetCode 15: 3Sum.
    Uses sorting and two pointers to find all unique triplets that sum to zero.
    Args:
        nums (List[int]): List of integers.
    Returns:
        List[List[int]]: List of unique triplets that sum to zero.
    """
    pass
